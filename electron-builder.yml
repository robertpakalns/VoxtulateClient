productName: Voxtulate Client
appId: pro.tricko.voxtulate
npmRebuild: false
directories:
  output: "dist"
artifactName: "${name}-${os}-${arch}.${ext}"

electronLanguages:
  - en-US

files:
  - "assets/**/*"
  # CSS and HTML is bundled inside JS bundles
  - "!assets/css/**/*"
  - "!assets/html/**/*"
  - "!src/**/*" # TypeScript code will not be executed by Node
  - "js-dist/**/*"
  - "!**/locales/*"
  - "!**/*.pak"
  - "!**/ffmpeg.dll"
  - "!**/d3dcompiler_47.dll"

win:
  target:
    - target: nsis
      arch:
        - x64
        - ia32
  icon: "assets/icons/icon.ico"

mac:
  target: dmg
  icon: "assets/icons/icon.icns"

linux:
  target:
    - AppImage
    - tar.gz
  icon: "assets/icons/icon.png"

nsis:
  oneClick: true
  deleteAppDataOnUninstall: true
  runAfterFinish: true
  uninstallDisplayName: "${productName}"
  shortcutName: "${productName}"
